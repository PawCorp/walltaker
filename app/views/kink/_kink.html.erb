<%# locals: (kink:, is_current_user: false, destination: :kink_show, link: nil) -%>

<%= turbo_frame_tag kink, class: ("kink #{kink.works_on_e621? ? 'valid' : 'untested'}"), target: destination == :search ? 'pornsearch' : '_top' do %>
  <%= link_to destination == :search ? porn_search_search_path(tags: kink.name, link:, full_rerender: true) : kink_show_path(kink), target: destination == :search ? 'pornsearch' : '_top' do %>
    <%= kink.name %>
  <% end %>

  <% if is_current_user %>
    <%= button_to kink_remove_path(kink), method: :delete do %>
      <ion-icon name="trash"></ion-icon>
    <% end %>
  <% end %>
<% end %>